# MaeroOS firewall rules (loaded by `fwctl reload` at boot).
# Disabled by default; uncomment to turn on.
# enable
# policy in allow
# policy out allow
# example: block outbound to a host:port
# rule drop out tcp 10.0.2.99/32 9999
